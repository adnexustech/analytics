import VideoAnalyticsPage from './VideoAnalyticsPage';
import { Metadata } from 'next';

export default function () {
  return <VideoAnalyticsPage />;
}

export const metadata: Metadata = {
  title: 'Video & CTV Analytics',
};
